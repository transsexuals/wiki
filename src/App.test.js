import { render, screen } from '@testing-library/react';
import App from './App';

// test('renders transsexual text', () => {
//   render(<App />);
//   const linkElement = screen.getByText(/transsexual/i);
//   expect(linkElement).toBeInTheDocument();
// });
